{
  "name": "peachplumdb",
  "version": "1.1.0",
  "description": "",
  "main": "appStarter.js",
  "type": "module",
  "bin": {
    "peach": "appStarter.js"
  },
  "directories": {
    "lib": "lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/splitscale/peachplumDb.git"
  },
  "bugs": {
    "url": "https://github.com/splitscale/peachplumDb/issues"
  },
  "homepage": "https://github.com/splitscale/peachplumDb#readme",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsc -w",
    "build": "npx tsc && echo Build output to: ./bin",
    "deploy": "npm run build && git add --all && git commit -m \"Automated commit step before version increment\" && npm version minor && git push && npm publish"
  },
  "author": "Jerome R. Cabugwason",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.16.3",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "reflect-metadata": "^0.1.13"
  }
}
